# pytest.ini  (放在项目根目录)
[pytest]
testpaths = tests/unit
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 让项目根目录加入 sys.path，确保能 import core.xxx
addopts = --html=tests/report.html --self-contained-html
            ; --cov=domains.energy --cov=core --cov=app
            ; --cov-report=html:tests/cov_html
            ; --cov-report=term-missing

# 👇 关键：把项目根目录加入 PYTHONPATH
pythonpath = .